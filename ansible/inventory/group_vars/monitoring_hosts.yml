---
host_firewall_rules:
  - port: "3000"
    comment: Grafana
  - port: "8080"
    comment: Kromgo
  - port: "9090"
    comment: Prometheus

pve_monitoring_token: "{{ lookup('community.general.onepassword', 'pve-monitoring-token', vault='Homelab', field='credential') }}"
proxmox_host: "10.15.1.10"
unifi_unpoller_user: "{{ lookup('community.general.onepassword', 'unpoller', vault='Homelab', field='username') }}"
unifi_unpoller_pass: "{{ lookup('community.general.onepassword', 'unpoller', vault='Homelab', field='password') }}"
